# Politica de respuesta.
# mode: enforce (bloquea) | monitor (solo observa y registra)
mode: enforce

# IPs o CIDR a bloquear. Los ejemplos usan rangos TEST-NET (RFC 5737), no reales.
blocklist:
  - 203.0.113.10
  - 198.51.100.0/24
